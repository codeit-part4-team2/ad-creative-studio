# Decisions

| Date | Status | Decision | Rationale | Evidence |
|---|---|---|---|---|
| 2026-08-10 | `[VERIFIED]` | Generate only the `fast_composite` background at 768x768; retain 1024x1024 final output and product canvas. | Reduce SDXL latent work without lowering customer-visible output or source product fidelity. | User approval and `docs/superpowers/specs/2026-08-10-model-speed-shorts-integration-design.md` |
| 2026-08-10 | `[VERIFIED]` | Keep model-serving and Shorts changes in separate review units. | Preserve PR #15 benchmark scope and keep FFmpeg/approval/YouTube review independent. | Current branches and approved design |
| 2026-08-10 | `[VERIFIED]` | Keep `fast_composite` at 4 steps by default. | The serving owner reported the lowest p50/p95 for 4 steps with the same 10/10 preservation response as 6 and 8 steps; final visual selection remains external. | User-approved final pre-push design and external report recorded in `model_server/README.md` |
| 2026-08-10 | `[VERIFIED]` | Inject `madebyollin/sdxl-vae-fp16-fix` into both model profiles. | Avoid stock SDXL VAE FP32 decode/upcast warnings while retaining the already-used quality-profile VAE. | `f22a65b`, focused RED/GREEN test, model card states MIT and `force_upcast=false` |
| 2026-08-10 | `[VERIFIED]` | Report GPU lock wait separately from generation. | Concurrent requests are intentionally serialized; operators need queue time separated from actual pipeline time. | `a286cf2`, focused concurrency/API tests |
| 2026-08-13 | `[VERIFIED]` | Route Shorts workflow logs through an application-owned stderr handler rather than a Uvicorn logger hierarchy. | The test VM currently uses direct Uvicorn terminals, but observability must survive a future Gunicorn or systemd transition. | Serving-owner command report plus server-independent subprocess regression test in PR #25. |
| 2026-08-13 | `[VERIFIED]` | Shorten rush-hour copy under `deadpan-ai-v3` instead of forcing about 1.6x playback or loosening the 15-second limit. | VM evidence identifies excess spoken copy, while aggressive speed-up would damage Korean pronunciation and deadpan timing. | Four WAV durations total 22.362 seconds; estimated total 23.962 seconds. |
| 2026-08-13 | `[VERIFIED]` | Use storyboard scene targets as minimum segment durations and share one timing calculation between validation and FFmpeg. | Short speech needs stable 11-second pacing, while validation and rendering must not drift apart. | Focused RED/GREEN tests and real FFmpeg integration at implementation commit `54e5712`. |
| 2026-08-13 | `[VERIFIED]` | Keep the PR #26 renderer unchanged and shorten only static self-aware/CTA text under `deadpan-ai-v4`. | The second L4 logs prove the shared duration calculation is correct; static lines consume 8.036 seconds and can be shortened without losing product facts. | Second E2E: estimated 15.965 seconds, speech 14.365 seconds, silence 1.600 seconds. |
| 2026-08-13 | `[VERIFIED]` | Replace the blurred duplicate background and centered image card with one centered full-bleed cover crop; keep captions boxless and shadowless with a 3px dark outline. | The supplied output showed the high-quality product image reduced to an inset card. A single full-frame image restores visual hierarchy while the outline preserves readability over light and dark backgrounds. | RED/GREEN frame tests, 1080x1920 preview, and `docs/superpowers/specs/2026-08-13-shorts-full-bleed-design.md`. |
| 2026-08-13 | `[VERIFIED]` | Persist the text-free model output as `source_image_url` and require it for Shorts instead of selecting a formatted SNS card. | Full-bleed magnifies any copy baked into the selected source. A separate source prevents image text from competing with video captions without another GPU call. | Expected RED failures, 320-test GREEN suite, and `docs/superpowers/specs/2026-08-13-shorts-clean-source-design.md`. |
| 2026-08-14 | `[VERIFIED]` | Persist clean sources only for rush-hour results and run persistence concurrently with copy generation; keep mock formatted placeholders per target format. | Normal time slots cannot create Shorts, so their raw source files are unused. Per-format mock placeholders avoid the white letterbox regression while the separate rush-hour source preserves the Shorts contract. | PR #28 review reproduction and focused RED/GREEN tests. |
| 2026-08-14 | `[VERIFIED]` | Do not add automatic focal cropping without a product mask or trusted focal metadata. | Color or edge heuristics can mistake background props for the product and worsen clipping. Actual four-scene crop safety remains a human E2E gate. | `docs/integration_checklist.md` and current renderer input contract. |
