# Known Issues

| Status | Issue | Reproduction | Impact | Evidence |
|---|---|---|---|---|
| `[INFERRED]` | The original `numpy==2.2.6` conflict is repaired locally with `numpy==2.3.5`, but a clean L4 install is not rerun yet. | Run clean Python 3.11 `pip install -r model_server/requirements.txt` and `pip check` on the L4 VM. | Fresh environment reproducibility is not yet externally confirmed. | Semantic regression test passes; large clean install intentionally not downloaded locally. |
| `[VERIFIED]` | Diffusers emits empty-list dtype warnings during IP-Adapter/VAE internal casts. | Load the previous quality or stock-fast path with Diffusers 0.39.0. | No observed image failure; warning text reports `[]`, so no retained FP32 modules are named. | Exact messages supplied by serving owner; Diffusers source path inspected. |
| `[UNVERIFIED]` | The new fast FP16-safe VAE has not run on L4. | Repeat 768/4-step with the current branch and same seed as the stock-VAE report. | Warning removal, latency, VRAM, and small visual differences are not established. | Local loader contract test passes; no local model weights were downloaded. |
| `[UNVERIFIED]` | B/C/D fast results were supplied as a chat report without original JSON or resolution metadata. | Compare benchmark response files against `docs/L4_BENCHMARK_CHECKLIST.md`. | Cannot attribute the reported speed specifically to 768 or bind it to an exact commit. | Reported values are recorded in the handoff; B0 and metadata remain missing. |
| `[UNVERIFIED]` | The final-main VM Shorts E2E reaches `processing` and then persists a generic render failure. | Deploy the render-stage logging change, create a new Shorts job, and inspect the `video render failed` traceback for that job ID. | The current API record cannot distinguish scene-image, TTS, or FFmpeg failure. | Operator report on 2026-08-13: `render_status=failed`, generic error, no TTS engine, no scene hashes, no video URL. |
| `[VERIFIED]` | The prior generic VM render failure was a duration-budget rejection, not a scene-image, TTS-runtime, or FFmpeg execution failure. | Job `video_906b946fcef1`: inspect four WAV durations and the `ffmpeg_render` traceback. | Previous `deadpan-ai-v2` copy produced 23.962 seconds and could never pass the 15.05-second maximum. | Operator measurements: 4.186, 6.016, 5.633, 6.527 seconds plus 1.600 seconds silence. |
| `[UNVERIFIED]` | Local tests cannot establish the real MeloTTS duration of `deadpan-ai-v3`. | Deploy the PR on L4 and generate the same product/time-slot E2E artifact. | The code removes the known copy overrun, but a long product name or selling point can still exceed the duration guard. | Local deterministic copy and synthetic-audio tests only; no model weights downloaded. |
| `[VERIFIED]` | Repository-wide Ruff currently reports an unrelated unused `os` import. | Run `python -m ruff check app tests` on `origin/main @ 096ea68`. | The broad lint command exits 1, although all files changed by the duration-budget PR are clean. | `tests/test_auth.py:2 F401`; changed-file Ruff exits 0. |
