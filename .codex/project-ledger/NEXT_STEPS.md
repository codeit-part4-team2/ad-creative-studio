# Next Steps

| Priority | Status | Action | Acceptance evidence | Blocker |
|---|---|---|---|---|
| 1 | `[VERIFIED]` | Review the 2026-08-10 design and plan. | User approval and committed design/plan. | None |
| 2 | `[VERIFIED]` | Fix dependency pins and add semantic compatibility regression coverage. | Focused test and full local gate pass. | Clean L4 environment resolution remains external. |
| 3 | `[VERIFIED]` | Add configurable 768 fast background, 1024 normalization, and explicit size metadata. | 175 tests plus Ruff, compileall, pip check, and diff check pass. | L4 timing and visual quality remain external. |
| 4 | `[VERIFIED]` | Reconcile the locally verified Shorts worktree with the model result contract. | Combined branch passes 244 tests plus Ruff, compileall, pip check, diff, credential, and ignored-artifact gates at `929c3be`. | None |
| 5 | `[VERIFIED]` | Add FP16-safe VAE and separate GPU queue timing. | Model branch passes 178 tests plus Ruff, compileall, pip check, and diff check. | L4 same-seed VAE comparison remains external. |
| 6 | `[UNVERIFIED]` | Push or update the remote PR only after explicit user authorization. | Remote branch contains the reviewed local commits and CI result is recorded. | Push intentionally withheld. |
| 7 | `[UNVERIFIED]` | Obtain the original B/C/D JSON and confirm `background_size=768`, `output_size=1024`. | Response metadata and exact execution commit are recorded. | Serving VM access. |
| 8 | `[UNVERIFIED]` | Run B0 1024/4-step versus B 768/4-step and stock versus FP16-safe VAE. | Same-seed P50/P95, stages, VRAM, warnings, failures, and image samples. | L4 access and model weights. |
| 9 | `[UNVERIFIED]` | Run clean Python 3.11 resolver and `pip check` on L4. | Full `model_server/requirements.txt` installs in one command and `pip check` exits 0. | L4 access and multi-GB dependencies. |
| 10 | `[UNVERIFIED]` | Review and deploy `codex/video-stage-logging`, then rerun one VM Shorts E2E job. | A `video render failed` record identifies `render_stage`, job/result IDs, exception type, and traceback. | Requires PR approval/merge and serving-owner deployment. |
| 11 | `[UNVERIFIED]` | Review and merge the Shorts duration-budget PR, then pull final `main` on the L4 VM. | VM commit matches the PR merge commit and both direct Uvicorn processes start with the expected `.env`. | PM approval and merge. |
| 12 | `[UNVERIFIED]` | Run a new rush-hour Shorts E2E job with MeloTTS. | `render_status=completed`, `video_url` is present, probed duration is 10~15 seconds, and stage logs contain no traceback. | L4 deployment and UI/E2E operator. |
| 13 | `[UNVERIFIED]` | Listen to all four lines and inspect the final video. | Korean pronunciation is accurate, deadpan copy sounds natural, captions match speech, and no scene or product is clipped. | Human audio/video review on the generated VM artifact. |
| 14 | `[UNVERIFIED]` | Review and merge the `deadpan-ai-v4` TTS-headroom PR, then pull final `main` on L4. | VM checkout matches the merge commit and the backend restarts successfully. | PM approval and merge. |
| 15 | `[UNVERIFIED]` | Rerun the same rush-hour E2E case after v4 deployment. | Speech plus silence is at most 15.05 seconds, `render_status=completed`, and a valid `video_url` is returned. | L4 deployment and UI/E2E operator. |
| 16 | `[VERIFIED]` | Commit, push, and open one full-bleed plus clean-source Draft PR after explicit user authorization. | Implementation commit `0e7137a` is on the team remote and Draft PR #28 targets `main`. | None. |
| 17 | `[UNVERIFIED]` | After merge and VM pull, rerun one Shorts E2E case and inspect all four scenes. | `render_status=completed`; 1080x1920 video has no blurred duplicate, inset card, or baked-in ad copy under captions; product remains visible; captions remain readable on light and dark regions. | PM approval, merge, serving-owner deployment, and human visual review. |
| 18 | `[UNVERIFIED]` | Compare current 768x768 source generation against a native portrait candidate in a separate L4 experiment. | Same input/seed reports P50/P95, peak VRAM, product preservation, crop loss, and blind visual preference before any production default changes. | L4 access and serving-owner coordination; intentionally excluded from tomorrow's visual PR. |
| 19 | `[VERIFIED]` | Obtain PM review and merge PR #28. | PM approved, GitHub pytest passed, and squash commit `806e4ca` is on `main`. | None. |
| 20 | `[VERIFIED]` | Review and publish the PR #28 follow-up as a Draft after explicit user authorization. | Commit `2b8587f` and Draft PR #35 contain the mock regression, bounded source storage, concurrent preparation, legacy UI guidance, tests, docs, and ledger updates based on `origin/main @ 9a938f6`. | None for Draft publication; CI and review remain separate gates. |
| 21 | `[UNVERIFIED]` | Obtain GitHub CI and PM review for Draft PR #35, then change readiness or merge only after explicit authorization. | Pytest passes, requested changes are resolved, PM approves, and a live PR read confirms the resulting state. | Current GitHub state: pytest in progress, review required, merge blocked. |
| 22 | `[UNVERIFIED]` | After follow-up merge and VM pull, generate a new rush-hour and normal-time advertisement and run one Shorts E2E job. | Rush result has a valid source URL, normal result has `null`, four scenes preserve the product, complex-background captions remain readable, duration is 10~15 seconds, and no OOM occurs. | Serving/UI operators and L4 environment. |
